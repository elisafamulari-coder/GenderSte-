# Repository Structure – GenderSte Survey Project
L'articolo in formato pdf, "Un dataset italiano per l'analisi degli stereotipi di genere nei testi mediante Large Language Model", contiene una spiegazione dettagliata di quanto svolto nell'analisi e il background teorico necessario per la compresione dell'analisi.

## Materiale per la creazione della survey
- **Zip `GenderSte_Survey`**  
  Contiene tutto il materiale utilizzato per la creazione della survey su PsyToolkit, tra cui:
  - file di testo delle sub-survey  
  - dataset di estratti testuali (.xlsx e .txt)  
  - script Python per la composizione automatica della survey  
  - output finale pronto per PsyToolkit  
---
## `AnalisiGenderSte_Persone`

- **Zip `PsyToolkitData_GenderSteSurvey_2026_02_11_08_55`**  
  Contiene i risultati del questionario svolto su PsyToolkit.
- **`Analisi-Gender_Ste-Survey`**
  Contiene un pdf con l'analisi dei dati raccolti nel questionario rivolto alle persone
---
## `AnalisiChatBot`

- **`gender_survey.pipeline.py`**
  Contiene lo script python utilizzato per interrogare i vari chatbot
- **`deepseek-chat`**  
  Contiene un pdf con l'analisi relativa ai dati raccolti nelle chiamate a deepseek e nella cartella 'data' i file delle varie run fatte con deepseek
- **`gemini-2.5-flash`**  
  Contiene un pdf con l'analisi relativa ai dati raccolti nelle chiamate a gemini e nella cartella 'data' i file delle varie run fatte con gemini
- **`gpt4.1-mini`**  
Contiene un pdf con l'analisi relativa ai dati raccolti nelle chiamate a gpt e nella cartella 'data' i file delle varie run fatte con gemini

---

## Analisi statistiche di confronto Umani vs Modelli

Le quattro zip seguenti contengono le analisi statistiche di confronto tra le valutazioni umane e quelle prodotte dai tre LLM (GPT-4.1-mini, Gemini-2.5-flash, DeepSeek-chat) sulle 111 sezioni testuali del dataset. Ogni cartella include lo script Quarto (`.qmd`), l'output compilato (`.pdf` e/o `.html` con relative figure) e i dataset CSV (`deepseek_wide_111.csv`, `gemini_wide_111.csv`, `gpt_wide_111.csv`, `human_wide_raw.csv`) usati come input.

- **Zip `Spearman`**
  Correlazione di Spearman tra i gender score assegnati dai valutatori umani e quelli prodotti dai tre modelli, scelta per la natura ordinale discreta della scala di valutazione (−2, −1, 0, +1, +2) e la robustezza rispetto a valori estremi. Include anche i file `risultati_spearman.csv` e `risultati_scores_unificati.csv` con i risultati numerici.

- **Zip `Paired_T-test_Bland-Altman`**
  Confronto tra i giudizi umani e quelli dei modelli AI tramite paired t-test e diagrammi di Bland–Altman, per valutare sia le differenze medie sia l'accordo/bias sistematico tra le valutazioni.

- **Zip `IRR`**
  Analisi dell'accordo inter-rater (IRR) tramite Alpha di Krippendorff (metodo ordinale), calcolata separatamente per i valutatori umani (accordo tra partecipanti) e per ciascun modello (stabilità tra le 5 run a diverse temperature: T=0, T=0.3×2, T=0.5×2), sia a livello globale che per categoria testuale (F, M, N).

- **Zip `Polarizzazione_ed_entropia`**
  Analisi della polarizzazione e dell'entropia dei giudizi (umani vs DeepSeek, Gemini, GPT) calcolate sui singoli rating (non sulle medie) lungo la scala a cinque categorie (−2, −1, 0, 1, 2).


# English version

The PDF article, *"An Italian Dataset for the Analysis of Gender Stereotypes in Texts Using Large Language Models"*, provides a detailed description of the study and the theoretical background required to understand the analyses.

## Survey Creation Material

* **`GenderSte_Survey.zip`**
  Contains all the material used to create the survey in PsyToolkit, including:

  * text files for the sub-surveys;
  * dataset of textual excerpts (`.xlsx` and `.txt`);
  * Python scripts for the automatic generation of the survey;
  * final output ready to be imported into PsyToolkit.

---

## `HumanGenderSte_Analysis`

* **`PsyToolkitData_GenderSteSurvey_2026_02_11_08_55.zip`**
  Contains the responses collected through the PsyToolkit questionnaire.

* **`Analisi-Gender_Ste-Survey`**
  Contains a PDF report describing the analysis of the data collected from the human participants.

---

## `ChatBotAnalysis`

* **`gender_survey_pipeline.py`**
  Python script used to query the different Large Language Models.

* **`deepseek-chat`**
  Contains a PDF report with the analysis of the responses generated by DeepSeek and, in the `data` folder, the files corresponding to the different experimental runs.

* **`gemini-2.5-flash`**
  Contains a PDF report with the analysis of the responses generated by Gemini and, in the `data` folder, the files corresponding to the different experimental runs.

* **`gpt4.1-mini`**
  Contains a PDF report with the analysis of the responses generated by GPT-4.1-mini and, in the `data` folder, the files corresponding to the different experimental runs.

---

## Statistical Analyses: Human vs. Model Comparison

The following four ZIP archives contain the statistical analyses comparing the evaluations provided by human participants with those produced by the three Large Language Models (GPT-4.1-mini, Gemini-2.5-flash, and DeepSeek-chat) on the 111 textual excerpts included in the dataset. Each archive contains the Quarto source file (`.qmd`), the compiled outputs (`.pdf` and/or `.html`, including figures), and the CSV datasets (`deepseek_wide_111.csv`, `gemini_wide_111.csv`, `gpt_wide_111.csv`, and `human_wide_raw.csv`) used as input.

* **`Spearman.zip`**
  Spearman's rank correlation between the gender scores assigned by human participants and those produced by the three models. This metric was chosen because the evaluation scale is discrete and ordinal (−2, −1, 0, +1, +2) and is robust to outliers. The archive also includes the files `risultati_spearman.csv` and `risultati_scores_unificati.csv`, containing the numerical results.

* **`Paired_T-test_Bland-Altman.zip`**
  Comparison between human evaluations and AI model predictions using paired t-tests and Bland–Altman plots to assess both mean differences and systematic agreement (bias) between the evaluations.

* **`IRR.zip`**
  Inter-rater reliability (IRR) analysis based on Krippendorff's Alpha for ordinal data. Reliability was computed separately for the human participants (agreement among raters) and for each model (consistency across the five runs performed at different temperatures: T=0, T=0.3 ×2, and T=0.5 ×2), both overall and for each textual category (Female, Male, and Neutral).

* **`Polarizzazione_ed_entropia.zip`**
  Analysis of judgment polarization and entropy (Humans vs. DeepSeek, Gemini, and GPT), computed on the individual ratings (rather than on the average scores) across the five-point evaluation scale (−2, −1, 0, +1, +2).







